rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // クライアントからの直接アクセスを全て拒否
    // 全データアクセスはCloud Functions（Admin SDK）経由のみ
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
